# Secrets
.env
.env.local

# OS
.DS_Store

# Python (Phase 1+)
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Node / Expo (Phase 1+)
node_modules/
.expo/

# Build artifacts
dist/

# User data (never commit uploads)
data/user_uploads/

# Downloaded model assets (version/checksum manifests are committed separately)
data/models/
